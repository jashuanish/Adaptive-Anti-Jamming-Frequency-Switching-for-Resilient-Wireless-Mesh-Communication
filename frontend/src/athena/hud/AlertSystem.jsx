/**
 * AlertSystem.jsx — DEPRECATED in new shell.
 * Alerts are handled by AlertToast (src/components/ui/AlertToast.jsx).
 * Kept as no-op to avoid breaking stale imports.
 */
export default function AlertSystem() {
  return null
}
