/**
 * ControlDeck.jsx — DEPRECATED in new shell.
 * Controls are now in ContextPanel (src/shell/ContextPanel.jsx).
 * Kept as no-op to avoid breaking stale imports.
 */
export default function ControlDeck() {
  return null
}
